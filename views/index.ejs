<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">
        <img src="<%= config.botAvatar || 'https://cdn.discordapp.com/embed/avatars/0.png' %>" alt="Bot Logo" class="navbar-logo">
        <%= config.botName || 'Discord Bot' %>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link active" href="/"><i class="bi bi-house-door"></i> Ana Sayfa</a>
          </li>
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard"><i class="bi bi-speedometer2"></i> Kontrol Paneli</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/logout"><i class="bi bi-box-arrow-right"></i> Çıkış</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/auth/discord"><i class="bi bi-discord"></i> Giriş</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <section class="hero">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-lg-7 animate-fade-in">
          <h1 class="mb-4"><%= config.botName || 'Discord Bot' %> <span style="color: var(--primary-color);">Dashboard</span></h1>
          <p class="lead mb-4 animate-slide-in animate-delay-1">Modern ve güçlü Discord botunuz için özelleştirilmiş kontrol paneli. Sunucunuzu yönetmenin en kolay yolu!</p>
          
          <div class="d-flex gap-3 animate-fade-in animate-delay-2">
            <% if (user) { %>
              <a href="/dashboard" class="btn btn-primary">
                <i class="bi bi-speedometer2 me-2"></i> Kontrol Paneline Git
              </a>
            <% } else { %>
              <a href="/auth/discord" class="btn btn-primary">
                <i class="bi bi-discord me-2"></i> Discord ile Giriş Yap
              </a>
            <% } %>
            <a href="<%= config.inviteUrl || '#' %>" class="btn btn-outline-light" target="_blank">
              <i class="bi bi-plus-circle me-2"></i> Botu Sunucuna Ekle
            </a>
          </div>
        </div>
        <div class="col-lg-5 text-center animate-fade-in animate-delay-3">
          <img src="<%= config.serverIcon || 'https://cdn.discordapp.com/embed/avatars/0.png' %>" alt="Server Logo" class="server-logo animate-float">
        </div>
      </div>
    </div>
  </section>

  <section class="py-5">
    <div class="container">
      <h2 class="text-center mb-5 animate-fade-in" style="color: var(--text-heading);">Bot İstatistikleri</h2>
      
      <div class="row">
        <div class="col-md-4 mb-4 animate-fade-in">
          <div class="stat-card">
            <i class="bi bi-people-fill stat-icon"></i>
            <div class="stat-value"><%= client.guilds.cache.get(config.guildId)?.memberCount || 0 %></div>
            <div class="stat-label">Toplam Üye</div>
          </div>
        </div>
        
        <div class="col-md-4 mb-4 animate-fade-in animate-delay-1">
          <div class="stat-card">
            <i class="bi bi-server stat-icon"></i>
            <div class="stat-value"><%= client.guilds.cache.size %></div>
            <div class="stat-label">Toplam Sunucu</div>
          </div>
        </div>
        
        <div class="col-md-4 mb-4 animate-fade-in animate-delay-2">
          <div class="stat-card">
            <i class="bi bi-chat-dots-fill stat-icon"></i>
            <div class="stat-value"><%= Math.floor(client.uptime / 86400000) %></div>
            <div class="stat-label">Çalışma Süresi (Gün)</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-5 bg-dark" style="background-color: var(--background-dark) !important;">
    <div class="container">
      <h2 class="text-center mb-5 animate-fade-in" style="color: var(--text-heading);">Bot Özellikleri</h2>
      
      <div class="row">
        <div class="col-md-4 mb-4 animate-fade-in">
          <div class="card h-100">
            <div class="card-body text-center">
              <i class="bi bi-shield-check display-4 mb-3"></i>
              <h5 class="card-title">Gelişmiş Moderasyon</h5>
              <p class="card-text">Sunucunuzu tam kontrol altında tutun. Uyarı, susturma, atma ve yasaklama komutları ile moderasyon işlemlerinizi kolayca yapın.</p>
            </div>
          </div>
        </div>
        
        <div class="col-md-4 mb-4 animate-fade-in animate-delay-1">
          <div class="card h-100">
            <div class="card-body text-center">
              <i class="bi bi-graph-up display-4 mb-3"></i>
              <h5 class="card-title">Seviye Sistemi</h5>
              <p class="card-text">Üyelerinizin aktifliğini takip edin ve ödüllendirin. Özel seviye kartları ve sıralama tablosu ile kullanıcılarınızı teşvik edin.</p>
            </div>
          </div>
        </div>
        
        <div class="col-md-4 mb-4 animate-fade-in animate-delay-2">
          <div class="card h-100">
            <div class="card-body text-center">
              <i class="bi bi-calendar-event display-4 mb-3"></i>
              <h5 class="card-title">Etkinlik Yönetimi</h5>
              <p class="card-text">Sunucunuzda etkinlikler oluşturun, yönetin ve takip edin. Katılımcıları otomatik olarak organize edin.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-5">
    <div class="container text-center animate-fade-in">
      <h2 class="mb-4" style="color: var(--text-heading);">Hemen Başlayın!</h2>
      <p class="lead mb-4">Discord sunucunuzu bir üst seviyeye taşımak için hemen botu ekleyin ve kontrol panelini kullanmaya başlayın.</p>
      
      <div class="d-flex justify-content-center gap-3">
        <a href="<%= config.inviteUrl || '#' %>" class="btn btn-primary btn-lg" target="_blank">
          <i class="bi bi-plus-circle me-2"></i> Botu Ekle
        </a>
        <a href="<%= config.supportServer || '#' %>" class="btn btn-outline-light btn-lg" target="_blank">
          <i class="bi bi-headset me-2"></i> Destek Sunucusu
        </a>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <div class="row">
        <div class="col-md-4 mb-4">
          <h5>Hakkımızda</h5>
          <p><%= config.botName || 'Discord Bot' %>, sunucularınız için modern ve özelleştirilebilir bir Discord botudur.</p>
          <div class="social-links mt-3">
            <a href="#"><i class="bi bi-discord"></i></a>
            <a href="#"><i class="bi bi-github"></i></a>
            <a href="#"><i class="bi bi-twitter-x"></i></a>
          </div>
        </div>
        
        <div class="col-md-4 mb-4">
          <h5>Hızlı Bağlantılar</h5>
          <ul class="list-unstyled">
            <li><a href="/"><i class="bi bi-chevron-right"></i> Ana Sayfa</a></li>
            <li><a href="/dashboard"><i class="bi bi-chevron-right"></i> Kontrol Paneli</a></li>
            <li><a href="<%= config.inviteUrl || '#' %>"><i class="bi bi-chevron-right"></i> Botu Davet Et</a></li>
            <li><a href="<%= config.supportServer || '#' %>"><i class="bi bi-chevron-right"></i> Destek Sunucusu</a></li>
          </ul>
        </div>
        
        <div class="col-md-4 mb-4">
          <h5>İletişim</h5>
          <ul class="list-unstyled">
            <li><i class="bi bi-envelope me-2"></i> destek@example.com</li>
            <li><i class="bi bi-discord me-2"></i> Discord: <%= config.ownerTag || 'Owner#0000' %></li>
          </ul>
        </div>
      </div>
      
      <hr style="border-color: rgba(255,255,255,0.1);">
      
      <div class="text-center py-3">
        <p class="mb-0">&copy; <%= new Date().getFullYear() %> <%= config.botName || 'Discord Bot' %>. Tüm Hakları Saklıdır.</p>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-fade-in');
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.1 });

      document.querySelectorAll('.card, .stat-card, h2, .lead').forEach(el => {
        if (!el.classList.contains('animate-fade-in')) {
          observer.observe(el);
        }
      });
    });
  </script>
</body>
</html> 